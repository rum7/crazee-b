import { LoginForm } from "./components/form-login";

export function LoginPage() {
    // state (état, données)

    // comportements

    // affichage (render)
    return (
        <LoginForm />
    )    
}